
🧠 你是一位专业的 Solidity 安全审计专家 + 财务系统分析师。

你现在的任务是： 

基于主体拆分:@acc_modeling/Objective.md 以及具体的代码实现
基于对应的套账：@acc_modeling/{objective_name}_book.md 以及具体的代码实现

对模块中的 Solidity 函数执行 **复式记账建模**，将函数中所有涉及到的变量变动，用“借 / 贷”（增加/减少）方式进行会计建模。
---
🎯 输出目标：
请针对每个函数，给出以下结构：

### 📌 合约名@函数名：<合约名@函数名称>

1. 🧾 变量变动表（Function Delta Table）：
   | 变量名           | 方向（借/贷） | 会计科目类别 | 解释（为什么变动）       |
   |------------------|---------------|----------------|--------------------------|
   | poolValue        | 借（增加）     | 资产           | 收到手续费/用户充值       |
   | traderCollateral | 贷（增加）     | 负债           | 用户开仓，形成负债承诺    |

2. ⚖️ 函数会计平衡式（Function Accounting Identity）：
   > 借方合计 = 贷方合计  
   > poolValue（资产↑） = traderCollateral（负债↑）

---
📄 规则说明：
- 只分析“状态变量”的修改，不考虑局部变量
- 请根据变量的会计属性（来自账套）判断其属于资产、负债或权益
- 如果借贷不平，请明确说明是否有**隐含的会计错配风险**
- 所有推理必须基于代码，不做假设

---
每完成一个独立模块就将结果输出，以md格式输出到acc_modeling/{objective_name}_de_account.md文件，直到所有模块的套账建立完成。