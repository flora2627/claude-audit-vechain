### 合约名@函数名：Stargate@stake

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| nativeBalance(VET)     | 借            | 资产         | 接收用户 VET（msg.value） |

2. 函数会计平衡式
> 借方合计（nativeBalance↑） = 贷方合计（用户对协议的投入，跨主体视为对外部的权益投入/委托准备）
注：无显式负债变量；需结合后续 `delegate()` 的资产重分类与跨主体恒等式校验。

---

### 合约名@函数名：Stargate@delegate

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| nativeBalance(VET)     | 贷            | 资产         | 将 VET 外转至 ProtocolStaker 进行委托 |

2. 函数会计平衡式
> 借方合计（对外委托资产形成） = 贷方合计（nativeBalance↓）
注：本合约不保留“对 ProtocolStaker 存款”型变量，资产重分类体现在跨主体恒等式。

---

### 合约名@函数名：Stargate@stakeAndDelegate

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| nativeBalance(VET)     | 借/贷（净 0） | 资产         | 先接收后外转委托，期末净额为 0 |

2. 函数会计平衡式
> 借方合计（接收） = 贷方合计（外转委托）

---

### 合约名@函数名：Stargate@migrateAndDelegate

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| nativeBalance(VET)     | 借/贷（净 0） | 资产         | 接收补足 VET 并立即外转委托 |

2. 函数会计平衡式
> 借方合计（接收） = 贷方合计（外转委托）

---

### 合约名@函数名：Stargate@unstake

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| nativeBalance(VET)     | 贷            | 资产         | 向用户返还质押 VET |

2. 函数会计平衡式
> 借方合计（解除对外委托/收回外部存款） = 贷方合计（nativeBalance↓、向用户支付）
注：若先前在 ProtocolStaker，函数内先通过外部交互取回后再支付。

---

### 合约名@函数名：Stargate@claimRewards

1. 变量变动表
| 变量名                 | 方向（借/贷） | 会计科目类别 | 解释 |
|------------------------|---------------|--------------|------|
| vthoBalance(VTHO_TOKEN)| 贷            | 资产         | 向用户发放 VTHO 奖励 |

2. 函数会计平衡式
> 借方合计（用户应收奖励结清） = 贷方合计（vthoBalance↓）
注：负债未显式入账，奖励应付按期间计算结清，需与跨主体恒等式核对。

---

### 合约名@函数名：Stargate@requestDelegationExit

1. 变量变动表
| 变量名                      | 方向（借/贷） | 会计科目类别 | 解释 |
|-----------------------------|---------------|--------------|------|
| delegatorsEffectiveStake(*) | 借/贷         | （待定）     | 期间有效质押快照调整，不属财务科目 |

2. 函数会计平衡式
> 不涉及资产/负债/权益金额变动；为分配权重类统计调整。


